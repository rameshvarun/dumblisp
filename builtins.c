
/* ARITHMATIC OPERATORS */

struct expr *plus(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *sub(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *mult(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *div(struct scope *scope, struct expr *arguments) {
  return NULL;
}

/* SYSTEM CALL WRAPPERS */

struct expr *exit(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *strlen(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *strcmp(struct scope *scope, struct expr *arguments) {
  return NULL;
}

struct expr *strcat(struct scope *scope, struct expr *arguments) {
  return NULL;
}
